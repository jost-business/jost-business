import { Component } from '@angular/core';

@Component({
  selector: 'app-trip-default',
  standalone: true,
  template: '<!-- Default trip detail (no extra content) -->',
})
export class TripDefaultComponent {}
